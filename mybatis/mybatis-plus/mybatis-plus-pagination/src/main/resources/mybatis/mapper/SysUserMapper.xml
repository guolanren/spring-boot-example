<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="name.guolanren.mapper.SysUserMapper">
    <select id="findPageXmlCustomize" resultType="SysUser">
      SELECT sys_user_id, name FROM example_sys_user WHERE name = #{name}
    </select>
</mapper>